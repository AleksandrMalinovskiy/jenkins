#!groovy
properties([disableConcurrentBuilds()])

pipeline {
    agent { 
        label 'main'
        }
    options {
        buildDiscarder(logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
        timestamps()
    }
    stages {
        stage("First step") {
            steps {
                sh 'ssh root@84.252.131.145 \'hostname\''
            }
        }
        stage("Second step") {
            steps {
                sh 'ssh root@84.252.131.145 \'uptime\''
            }
        }
    }
}